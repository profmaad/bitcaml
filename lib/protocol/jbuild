(library
 ((name bitcoin_protocol)
  (libraries (bitstring
	      bignum
	      core
	      bitcaml_utils
	      bitcoin_crypto
	      bitcoin_consensus))
  (preprocess (((pps (JANE ppx_tools ppx_bitstring)) All)))
  ))
