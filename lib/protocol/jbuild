(library
 ((name bitcoin_protocol)
  (libraries (bitstring
	      sha
	      cryptokit
	      microecc
	      core
	      bitcaml_utils
	      bitcoin_crypto))
  (preprocess (((pps (JANE ppx_tools ppx_bitstring)) All)))
  ))
