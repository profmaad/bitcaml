(library
 ((name bitcoin_blockchain)
  (libraries (core
	      bitcaml_utils
	      bitcoin_protocol
	      bitcoin_script
	      sqlexpr
	      sqlexpr.ppx))
  (preprocess (((pps (JANE ppx_tools re.pcre sqlexpr.ppx)) All)))
  ))
