(library
 ((name bitcoin_blockchain)
  (libraries (core
	      bignum
	      bitcaml_utils
	      bitcoin_protocol
	      bitcoin_script
	      bitcoin_crypto))
  (preprocess (((pps (JANE)) All)))
  ))
